import{_ as e,c as a,o as t,V as s}from"./chunks/framework.7Wm2Ym07.js";const f=JSON.parse(`{"title":"Graph Schema's","description":"","frontmatter":{},"headers":[],"relativePath":"guide/schemas.md","filePath":"guide/schemas.md"}`),r={name:"guide/schemas.md"},o=s('<h1 id="graph-schema-s" tabindex="-1">Graph Schema&#39;s <a class="header-anchor" href="#graph-schema-s" aria-label="Permalink to &quot;Graph Schema&#39;s&quot;">​</a></h1><p>A graph database schema is a conceptual representation of the data stored in a graph database, including the types of relationships between data entities and the properties or attributes of those entities.</p><p>Unlike relational databases which typically use a static, predefined schema, graph databases often employ a schema-less or &quot;schema-optional&quot; design. In fact, this applies to most NoSQL databases. This means that while you may define a schema for your data, it&#39;s not mandatory, and it can be easily adjusted to accommodate evolving data requirements. This flexibility makes graph databases well-suited to modelling complex and interconnected data.</p><p>When using Qwiery the schema will, hence, be inferred from the data. Depending on the adapter, this means that the schema has to be harvested from all nodes and their properties (this is e.g. the case with the JSON adapter) or fetched from an internal call (e.g. Neo4j with a call to <a href="https://neo4j.com/docs/operations-manual/current/reference/procedures/#procedure_db_schema_visualization" target="_blank" rel="noreferrer">db.schema.visualization</a>).</p><p>As such, one does not design a schema for a graph database and it does typically not enforce anything. The <a href="/clients/nuxt/schemaviz">Qwiery schema visualization</a> is purely informative. Some graph databases (e.g. <a href="https://tigergraph.com" target="_blank" rel="noreferrer">TigerGraph</a> and <a href="https://typedb.com" target="_blank" rel="noreferrer">TypDB</a>) do have strict schema adherence but most do not.</p><div class="info custom-block"><p class="custom-block-title">Schema Plugin</p><p>If you do wish to have a schema protected graph you can use the <a href="/plugins/schema/">schema plugin</a> which enforces a predefined schema. It effectively intercepts calls to and from the underlying storage to ensure it matches the schema.</p></div>',6),i=[o];function n(h,c,d,l,p,m){return t(),a("div",null,i)}const g=e(r,[["render",n]]);export{f as __pageData,g as default};
